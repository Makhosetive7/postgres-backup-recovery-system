# Retention policy (implemented by scripts/retention_policy.sh)
# Values below mirror .env — edit .env as the source of truth.

DAILY_RETENTION=7    # keep all backups from last 7 days
WEEKLY_RETENTION=4   # keep newest backup per week for 4 weeks
MONTHLY_RETENTION=3  # keep newest backup per month for 3 months
